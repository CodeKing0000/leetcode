class Solution {
public:
    int countStudents(vector<int>& students, vector<int>& sandwiches) {
        std::queue<int> students_q
        for(int i = 0; i < students.size() - 1; ++i) {
            for(int j = 0; j < sandwiches.size() - 1; ++j) {
                if(i != j) {
                    students_q.push(i)
                }
            }
        }
        if(students_q.push(i).empty() == true) {
            
        }
    }
};